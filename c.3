#include <stdio.h>
#include <string.h>

int next_permutation(char *s[], int n) {
    int i = n - 2;

    // 1. Find longest non-increasing suffix
    while (i >= 0 && strcmp(s[i], s[i + 1]) >= 0) {
        i--;
    }
    if (i < 0) {
        return 0;  // last permutation reached
    }

    // 2. Find rightmost successor to pivot
    int j = n - 1;
    while (strcmp(s[j], s[i]) <= 0) {
        j--;
    }

    // 3. Swap pivot and successor
    char *tmp = s[i];
    s[i] = s[j];
    s[j] = tmp;

    // 4. Reverse suffix
    int l = i + 1, r = n - 1;
    while (l < r) {
        tmp = s[l];
        s[l] = s[r];
        s[r] = tmp;
        l++;
        r--;
    }

    return 1;
}

int main(void) {
    int n;
    if (scanf("%d", &n) != 1) return 0;

    char arr[100][101];     // adjust sizes if needed
    char *s[100];
    for (int i = 0; i < n; i++) {
        scanf("%100s", arr[i]);
        s[i] = arr[i];
    }

    // The input is already sorted lexicographically as per problem statement.
    // Print the first (current) permutation:
    do {
        for (int i = 0; i < n; i++) {
            if (i) printf(" ");
            printf("%s", s[i]);
        }
        printf("\n");
    } while (next_permutation(s, n));

    return 0;
}
