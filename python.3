if __name__ == '__main__':
    N = int(input().strip())
    lst = []
    for _ in range(N):
        parts = input().split()
        cmd = parts[0]

        if cmd == "insert":
            i = int(parts[1])
            e = int(parts[2])
            lst.insert(i, e)
        elif cmd == "print":
            print(lst)
        elif cmd == "remove":
            e = int(parts[1])
            lst.remove(e)
        elif cmd == "append":
            e = int(parts[1])
            lst.append(e)
        elif cmd == "sort":
            lst.sort()
        elif cmd == "pop":
            lst.pop()
        elif cmd == "reverse":
            lst.reverse()
