#include <stdio.h>

int main(void) {
    int n;
    scanf("%d", &n);

    int i = 1;
    int sum = 0;

    while (i <= n) {
        if (i % 2 == 0) {      // check even
            sum += i;
        }
        i++;
    }

    printf("%d\n", sum);

    return 0;
}
